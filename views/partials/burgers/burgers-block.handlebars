<li>
	{{name}}

	<button class="change-devoured" data-id="{{id}}" data-newBurger="{{devoured}}">
		{{#if devoured}}DEVOURED!{{else}}RE-MAKE{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
